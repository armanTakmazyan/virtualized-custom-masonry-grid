import { HeaderContainer } from './styles';
import { HeaderFactory } from './HeaderFactory';

export const Header = () => {
  return (
    <HeaderContainer>
      <HeaderFactory />
    </HeaderContainer>
  );
};
